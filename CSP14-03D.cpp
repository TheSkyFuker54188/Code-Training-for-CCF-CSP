#include <iostream>
#include <vector>
#include <queue>

using namespace std;

struct Point
{
    long long x, y;
    bool is_extra;
};

struct Node
{
    int id;
    int cnt;
    int dist;
};

Point points[205];
bool visited[205][205];

int main()
{
    int n, m, k;
    long long r;
    if (!(cin >> n >> m >> k >> r))
        return 0;

    for (int i = 0; i < n; i++)
    {
        
    }
    for (int i = n; i < n + m; i++)
    {
        
    }
}
