// 2020-9-C 点亮数字人生
// 拓扑排序 模拟
#include <iostream>
#include <queue>
#include <string>
#include <utility>
#include <vector>

using namespace std;

int main()
{
    int Q;
    cin >> Q;
    for (int i = 0; i < Q; i++)
    {
        int M, N;
        cin >> M >> N;
        for (int j = 0; j < N; j++)
        {
            string FUNC;
            int k;
            cin >> FUNC >> k;
            string L[k];
            for (int v = 0; v < k; v++)
            {
                cin >> L[v];
            }
        }
    }
}