// 2013年12月 第D题
// 涉及考点：算法 + 数据结构）

// todo  组合数学解法
#include <iostream>
#include <vector>
using namespace std;
const int N = 2e5 + 10;
#define int long long
int t, n, mod = 1e9 + 7, c[1001][1001];
int solve(int x)
{
    if (x < 3)
        return 0;
    int ans = 0;
    for (int i = 1; i <= x - 2; i++)
    {
        int k = x - i;
        ans = (ans + c[x][i] * i * (k - 1)) % mod;
    }
    return ans;
}
signed main()
{
    cin >> t;
    for (int i = 0; i <= 1000; i++)
        c[i][0] = c[i][i] = 1;
    for (int i = 1; i <= 1000; i++)
    {
        for (int j = 1; j < i; j++)
            c[i][j] = (c[i - 1][j - 1] + c[i - 1][j]) % mod;
    }
    while (t--)
    {
        cin >> n;
        cout << solve(n - 1) << endl;
    }
}

/*
todo  组合数学解法

### 1. 核心大前提：确定最高位必须是 2
题目有四个条件：
1. 包含 0, 1, 2, 3 各至少一次。
2. 0 必须全在 1 前面。
3. 2 必须全在 3 前面。
4. 首位不能是 0。

我们来想一下，**最高位（第一位）到底可以排什么数字**？
* **不能是 0**：条件 4 规定。
* **不能是 1**：因为所有的 0 必须排在 1 前面，如果把 1 放最高位，0 就没法排在 1 前面了。
* **不能是 3**：同理，所有的 2 必须排在 3 前面，最高位放 3 的话 2 就排不在它前面了。

这就得出了整个解法最核心的推论：**这 $n$ 位的数字，它的最高位（第 1 位）必定是 2 ！**

### 2. 剩下位置的分配
最高位已经用了 1 个 `2`，所以总共 $n$ 位数字，只剩下 $x = n - 1$ 个位置需要填充（这就是代码中 `int solve(int x)` 传入 `n - 1` 的原因）。

剩下的这 $x$ 个位置，我们要把它们**分成两组**来放剩下的数字：
* **第一组**：用来放剩余的 `2` 和必带的 `3`（假设选了 $i$ 个位置）
* **第二组**：用来放 `0` 和 `1` （剩下的 $k = x - i$ 个位置）

### 3. 排列组合推导
代码里的 `for(int i = 1; i <= x - 2; i++)` 枚举的就是**第一组（放2和3）所占的位置个数 $i$**。

我们分三步来看每种分配策略有多少种情况：

**① 挑位置：`C[x][i]`**
我们要从 $x$ 个空位中，挑出 $i$ 个位置给“2和3”用，根据组合数，有 $C_x^i$ 种挑法。剩下 $k = x - i$ 个位置自然就是留给 “0和1” 的。

**② 排第一组（2和3）：有 `i` 种排法**
* 这 $i$ 个位置只放 `2` 和 `3`，且 `2` 必须排在 `3` 前面。
* 因为最高位已经有了一个 `2`（满足了“至少存在一个2”的要求），所以这 $i$ 个位置里**可以没有 `2`，但必须至少有一个 `3`**。
* 那么 `3` 的个数可以是 1个, 2个, ..., 一直到 $i$ 个（剩下的位置全补 `2` 并放在前面）。
* 所以这 $i$ 个位置一共有 **$i$** 种合法的排法。

**③ 排第二组（0和1）：有 `k - 1` 种排法**
* 剩下的 $k$ 个位置留给 `0` 和 `1`，且 `0` 必须排在 `1` 前面。
* 这次因为前面没有 `0` 和 `1`，所以我们**必须保证至少有一个 `0` 和一个 `1`**。
* 想象这 $k$ 个位置排成一排，我们只需要在中间切一刀，左边全放 `0`，右边全放 `1` 即可。
* $k$ 个槽位中间有 $k - 1$ 个间隔可以切（相当于选择0和1的分界线）。
* 所以这 $k$ 个位置一共有 **$k - 1$** 种合法的排法。

### 4. 边界范围为什么是 `i <= x - 2` ？
* 因为第二组（放0和1的地方）必须要满足“至少有一个0和一个1”，所以 $k$ （也就是 $x - i$） 至少得等于 2。
* 即 $x - i \ge 2$，推导出 $i \le x - 2$。
* 反之，第一组必须要放至少一个 3，所以 $i \ge 1$。
这就解释了代码里循环写 `for(int i = 1; i <= x - 2; i++)` 的原因。

### 总结
对于每一个可能的 $i$，符合条件的数字个数就是三部曲相乘：
**位置挑选方案数 × (2,3)的内部排法数 × (0,1)的内部排法数**

转换成代码就是：
`c[x][i] * i * (k - 1)`

累加这个结果并对 $10^9+7$ 取模，就是最终的答案。这个解法利用数学直接避开了复杂的动态规划或暴力搜索，是一个极其巧妙的时间复杂度为 $O(n)$ （配合组合数预处理）的最优解。
*/