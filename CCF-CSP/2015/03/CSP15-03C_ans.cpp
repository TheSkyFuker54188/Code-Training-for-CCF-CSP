// 2015年03月 第C题
// 涉及考点：算法 + 数据结构）

#include <iostream>
#include <cstdio>

using namespace std;

int m_day[] = {31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31}; //?  预处理天数
int month[15];                                                  // month[i]代表前i个月的累计天数

bool Leap(int n)
{
    if (n % 4 == 0 && n % 100 != 0 || n % 400 == 0) // 判断是否是闰年
    {
        return true;
    }
    return false;
}

int main()
{
    int a, b, c, y1, y2;
    cin >> a >> b >> c >> y1 >> y2;

    int days = 0; // todo  总天数（从 1850 年到目标年份 y1 前一年的总天数偏移）

    //?  定位起始年（1850年）： 以 1850-01-01（星期二）为原点。

    //?  计算目标年 1 月 1 日的星期
    /*
    遍历 1850 到y1 - 1年
    每年加 365 天，若是闰年额外加 1 天
    结果对 7 取模
    */
    for (int i = 1850; i < y1; i++) //* 从1850年开始算累计多少天
    {
        if (Leap(i))
            days += 366;
        else
            days += 365;
    }
    days %= 7;

    month[0] = 0;
    for (int i = 1; i <= 12; i++)
    {
        month[i] = month[i - 1] + m_day[i - 1];
    }

    // todo  星期的计算通常使用 (初始星期 + 偏移天数 - 1) % 7 + 1

    //?  计算目标年 1 月 1 日的星期
    int week = (2 + days - 1) % 7 + 1; //* y1年的1月1日是星期几

    //?  逐年处理 y1到y2
    /*
    确定 a 月 1 日的星期。 可以根据该年 1 月 1 日的星期加上前 a-1 个月的天数偏移
    确定 a 月总天数。 注意判断该年是否为闰年且 a=2
    遍历窗口寻解。 遍历 a 月的每一天，维护一个变量记录当前是星期几，遇到星期 c 就计数，直到计满 b 次
    边界检查。 如果遍历完该月计数还没到 b，输出 none
    滚动更新。 算出该年总天数，更新下一年 1 月 1 日的星期
    */
    for (int i = y1; i <= y2; i++)
    {
        for (int j = 1; j <= 12; j++) // 逐月遍历
        {
            if (j == a) // a月
            {
                bool flag = false;
                int week1 = (week + month[j - 1] - 1) % 7 + 1;
                if (j >= 3 && Leap(i)) // 如果是闰年，2 月之后的所有月份相较于平年都会多偏移 1 天。
                    week1 = week1 % 7 + 1;

                int len = m_day[j - 1] + (j == 2 && Leap(i) ? 1 : 0); // 这个月(考虑闰)
                int sum = 0;                                          // 计数器：当前是第几个 "星期c"

                for (int k = 1; k <= len; k++) // 逐天遍历
                {
                    if (week1 == c) // 星期c
                    {
                        if (++sum == b) // 第b个星期
                            flag = true, printf("%04d/%02d/%02d\n", i, j, k);
                    } // 格式化输出：自动补全前导零
                    week1 = week1 % 7 + 1; // 天数增1，星期随着增1
                }
                if (!flag)
                    cout << "none" << endl;
            }
        }
        week = (week + 365 + Leap(i) - 1) % 7 + 1; // 更新下一年1月1日是星期几
    }
    return 0;
}

/*
### 1. 核心逻辑解析

代码通过以下几个步骤来解决问题：

#### A. 基准点确定
题目给出 1850 年 1 月 1 日是星期二。代码中 `days` 变量用于记录从 1850 年到目标年份 `y1` 前一年的**总天数偏移**。

#### B. 星期计算公式
星期的计算通常使用 `(初始星期 + 偏移天数 - 1) % 7 + 1`。
- 因为 1850 年 1 月 1 日是星期二，所以初始值设为 `2`。
- `(2 + days - 1) % 7 + 1` 就能算出 `y1` 年 1 月 1 日是星期几。

#### C. 月份与闰年处理
- `m_day` 数组存储平年每月天数。
- `Leap` 函数判断闰年（400 的倍数，或 4 的倍数且非 100 的倍数）。
- 特别注意：如果是闰年，2 月之后的所有月份相较于平年都会多偏移 1 天。

#### D. 寻找第 $b$ 个星期 $c$
在特定的年份 $i$ 和月份 $a$ 中：
1. 算出该月 1 号是星期几。
2. 遍历该月每一天（1 号到 `len` 号）。
3. 用一个计数器 `sum` 记录当前是第几个“星期 $c$”。
4. 当 `sum == b` 时，当前的日期 `k` 就是我们要找的答案。

---

### 2. 做题思路总结

如果你要自己重新写一遍，建议遵循以下流程：

1.  **预处理天数：** 创建一个函数或数组处理平闰年每个月的天数。
2.  **定位起始年（1850年）：** 以 1850-01-01（星期二）为原点。
3.  **计算目标年 1 月 1 日的星期：**
    - 遍历 1850 到 $y_1-1$ 年。
    - 每年加 365 天，若是闰年额外加 1 天。
    - 结果对 7 取模。
4.  **逐年处理（从 $y_1$ 到 $y_2$）：**
    - **Step 1: 确定 $a$ 月 1 日的星期。** 可以根据该年 1 月 1 日的星期加上前 $a-1$ 个月的天数偏移。
    - **Step 2: 确定 $a$ 月总天数。** 注意判断该年是否为闰年且 $a=2$。
    - **Step 3: 遍历窗口寻解。** 遍历 $a$ 月的每一天，维护一个变量记录当前是星期几，遇到星期 $c$ 就计数，直到计满 $b$ 次。
    - **Step 4: 边界检查。** 如果遍历完该月计数还没到 $b$，输出 `none`。
    - **Step 5: 滚动更新。** 算出该年总天数，更新下一年 1 月 1 日的星期。

### 3. 代码中的关键点（易错点）
- **格式化输出：** 使用 `printf("%04d/%02d/%02d\n", ...)` 自动补全前导零（比如 `05` 月 `09` 日）。
- **闰年 2 月：** 闰年 2 月是 29 天，这会影响到之后月份的星期起始点。
- **星期转换：** 星期从 1-7 循环。逻辑写成 `week = week % 7 + 1` 可以保证在 7 之后回到 1。

这种“暴力模拟”的方法在 $y \le 2050$ 的规模下效率非常高，是处理日期类题目最通用的手段。
*/